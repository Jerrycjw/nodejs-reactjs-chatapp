{
  "name": "foobar-app",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix app",
    "heroku-postbuild": "npm run build --prefix app && npm run dev --prefix server"
  },
  "engines": {
    "node": "8.9.4"
  }
}