{
  "name": "foobar-app",
  "version": "0.1.0",
  "scripts": {
    "start": "npm start --prefix server",
    "postinstall": "npm install --prefix server && npm install --prefix app",
    "heroku-postbuild": "cd app && npm start --prefix app"
  },
  "engines": {
    "node": "8.9.4"
  }
}